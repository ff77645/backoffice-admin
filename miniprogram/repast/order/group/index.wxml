<view class="px-4 group">
  <view class="text-sm text-gray-500 sticky top-0 bg-white" style="z-index: 100;">{{data.title}}</view>
  <group-item
    wx:for="{{data.children}}" 
    wx:key="index" 
    item="{{item}}"
    bind:change="onChange"
  ></group-item>
  <!-- <view wx:for="{{data.children}}" wx:key="index" class="py-3">
  </view> -->
</view>